/** 
* @see https://github.com/password-cockpit/frontend for the canonical source repository 
* @copyright Copyright (c) 2018 Blackpoints AG (https://www.blackpoints.ch) 
* @license https://github.com/password-cockpit/frontend/blob/master/LICENSE.md BSD 3-Clause License 
*/

import { module, skip } from 'qunit';
import { setupTest } from 'ember-qunit';

module('Unit | Service | close-folders-inputs', function (hooks) {
    setupTest(hooks);

    skip('skip -  init test', function(assert) {
        assert.ok(true)
    });

    skip('skip -  closeAllInputs() test', function(assert) {
        assert.ok(true)
    });

    skip('skip -  closeFoldersInputs() test', function(assert) {
        assert.ok(true)
    });

    skip('skip -  closeFolderUserInputs() test', function(assert) {
        assert.ok(true)
    });
});