{{#if this.session.isAuthenticated}} 
	<HeadNavigation @session={{this.session}} @user={{this.model.user}} @permission={{this.model.permission}} @isLdap={{this.model.isLdap}} /> 
{{/if}}
{{outlet}}

<BrowserError />