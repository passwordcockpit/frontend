{{#if this.canViewLog}}
    <div class="panel panel-default log">
        <div class="panel-heading">
            {{t 'User\'s log'}}
        </div>
        <div class="panel-body">
            {{#if this.loading}}
                <p class="alert alert-info">
                    <i class="fas fa-info-circle"></i>
                    {{t 'Loading ...'}}
                </p>
            {{else if this.logs}}
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>
                                    {{t 'Date'}}
                                </th>
                                <th>
                                    {{t 'Folder\'s name'}}
                                </th>
                                <th>
                                    {{t 'Password\'s title'}}
                                </th>
                                <th>
                                    {{t 'Action'}}
                                </th>
                            </tr>
                        </thead>
                        <tbody>
                            {{#each this.logs as |log|}}
                                <tr>
                                    <td class="grid-150">
                                        {{log.action_date}}
                                    </td>
                                    <td>
                                        {{log.password_folder}}
                                    </td>
                                    <td>
                                        {{log.password_title}}
                                    </td>
                                    <td>
                                        {{log.action}}
                                    </td>
                                </tr>
                            {{/each}}
                        </tbody>
                    </table>
                </div>
            {{else}}
                <p class="alert alert-info">
                    <i class="fas fa-info-circle"></i>
                    {{t 'There are no logs'}}
                </p>
            {{/if}}
        </div>
        {{#if this.logs}}
            <LogPaginator @page={{this.page}} @pageCount={{this.pageCount}} @changePage={{action "submit"}} />
        {{/if}}
    </div>
{{/if}}
{{yield}}