{{#if this.foldersController.searchResults.hasResults}}
    <main class="main-password">
    </main>
{{else}}
    <Password::PasswordList @showPasswordsList={{this.showPasswordsList}} @hidePasswordsList={{this.hidePasswordsList}} @showList={{this.showList}} />
    <main class="main-password">
        {{outlet}}
    </main>
{{/if}}